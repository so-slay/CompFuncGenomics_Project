# Config/Input File for CFG: Markov Project.

# Specify input location. The FASTA files will be written to the same folder
# For folders: use this format:
input_dir = ""

# If needed, specify files. Default, all *.tsv will be run
# Separate files with commas
files = ""

# Specify order "m" for Markov chain
markov_order = "4"

# Specify k for k-fold Cross Validation
k_fold = 3

