# Config File for CFG: Markov Project.
# Specify input location. The FASTA files and other outputs will be written to subfolders of this same folder
# For folders: use this format:
input_dir = /home/s_vanantha/Desktop/IISERpBin/IISERP_Sem8/CfG/projectData
# If needed, specify files. Default, all *.tsv will be run. Separate file names with commas
files = chr4_200bp_bins.tsv
# Specify order "m" for Markov chain
markov_order = 3
# Specify k for k-fold Cross Validation
k_fold = 5
# Specify which factor: Choose from CTCF, REST or EP300
which_factor = CTCF
# This is needed only if you want to run on New data: i.e fetch FASTA from tsv
# Specify path to hg38 genome on your machine
ref_genome =


# Note: do not use "quotes" on filenames or other arguments
